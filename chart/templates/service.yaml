apiVersion: v1
kind: Service
metadata:
  name: {{ include "demochart.fullname" . }}
  labels:
    app : "{{ .Values.appname }}"
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
  selector:
    app : "{{ .Values.appname }}"
